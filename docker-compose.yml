services:
  db:
    image: postgres:16
    container_name: codera-db
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - codera_data:/var/lib/postgresql/data

volumes:
  codera_data: